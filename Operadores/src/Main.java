public class Main {
    public static void main(String[] args) {
        /*
        String  minhaString = "Like a Bosch"; // Um sinal de igual é atribuição
        //Operador relacional - sempre retorna valores booleanos - true ou false
        System.out.println(10 == 5 + 5); // Dois sinais de igual é igualdade
        System.out.println('A' == 'a');
        System.out.println(minhaString == "Like a Bosch");
         */

        /*
        String  minhaString;
        Scanner scanner = new Scanner(System.in);
        minhaString = scanner.nextLine();
        System.out.println(minhaString.equals("Like a Bosch"));
        */

        // Operadores Relacionais - Interprete como perguntas
        // == Igualdade
        // != Diferente de
        // > Maior que
        // < Menor que
        // >= Maior ou igual que
        // <= Menor ou igual que

        // Operadores Lógicos
        // Binários
            // 1 - True
            // 0 - False
                // AND - &&
                // OR - ||
        // Unário
            // NOT - ! Inversor lógico


        /*
        // Incremento
        int x = 5;
        int y = 8;

        System.out.printf("X = %d | y = %d\n", x, y);

        // Tipos de incremento
        x++;
        y--;
        System.out.printf("X = %d | y = %d\n", x, y);

        x+=1;
        y-=1;
        System.out.printf("X = %d | y = %d\n", x, y);

        x = x + 1;
        y = y - 1;
        System.out.printf("X = %d | y = %d\n", x, y);
         */
    }
}